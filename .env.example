# PXGuard – SMTP credentials for email alerts.
# Copy to .env and fill in your values. Do NOT commit .env to git.
# All four variables are REQUIRED when email_alerts_enabled: true in config.yaml.

PXGUARD_SMTP_HOST=
PXGUARD_SMTP_PORT=
PXGUARD_SMTP_USER=
PXGUARD_SMTP_PASSWORD=
